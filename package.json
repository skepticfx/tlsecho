{
  "name": "tlsecho",
  "version": "0.3.0",
  "description": "A simple TLS echo server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "^2.0.0"
  }
}
